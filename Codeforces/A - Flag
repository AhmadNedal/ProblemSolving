#include <bits/stdc++.h>
#define ll long long
#define SPEED ios::sync_with_stdio(0); cin.tie(0);
#define YES cout << "YES"<<endl;
#define NO cout << "NO"<<endl;
#define IN(vec) for(auto&it:vec) cin >> it;
using namespace std;


ll const MOD =1e9+7;

vector<ll>v,seg,arr,v2;
ll n,x,m,o,l,k,ans=0,mn=LLONG_MAX,r=MOD,y;
string s,t;
ll dp[1001];





void solve() {

    cin>>n>>m ;
    char a,l='i';
    int f = 1 ;
    while(n--) {
        cin>>s;

        a=s[0];
        if(a==l)f=0;
        
        for (auto it : s )if(it!=a)f=0 ;
        l=a;
    }

    if ( !f )NO
    else YES;






}

int main() {
    SPEED;

    int t=1;
    while (t--)solve();

    return 0;
}
