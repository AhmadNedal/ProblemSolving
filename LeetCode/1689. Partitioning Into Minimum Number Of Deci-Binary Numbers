class Solution {
public:
    int minPartitions(string n) {
        int maxnum =0 ; 

        for (auto it : n ) {
            maxnum = max(it-'0', maxnum) ; 
        }
        return maxnum ; 
    }
};
