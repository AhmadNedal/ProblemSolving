class Solution {
public:
    int balancedStringSplit(string s) {
        int num=0, counter=0 ,num2=0;

        for (auto it : s ) {
            if ( it == 'L')num++; 
            if ( it == 'R')num2++; 

            if ( num==num2)counter+=1; 
        }
        return counter ; 
    }

};
